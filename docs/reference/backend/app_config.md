---
title: backend.app_config
---

# `backend.app_config`

App-wide config: the Komga root + default output directory the UI's Config
tab edits. Persisted in the `jobs.db` SQLite file.

::: backend.app_config.router

::: backend.app_config.service

::: backend.app_config.exceptions
